/**
 * Base path for assets when the site is not served from domain root.
 * Leave "" for soyplaneta.org, Netlify/Vercel default, or GitHub Pages (user site).
 * For GitHub Project Pages (https://user.github.io/REPO/), set to "/REPO".
 */
window.SOYPLANETA_BASE = window.SOYPLANETA_BASE || "";
